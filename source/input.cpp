#include <SDL.h>
#include <SDL_image.h>
#include <SDL_ttf.h>

#include <switch.h>

#include "Input.hpp"
#include "Cursor.hpp"

Input::Input(){

}

int Input::checkInput()
{
	/*while (SDL_PollEvent(&event))
	{
		switch (event.type)
		{
			case SDL_JOYBUTTONDOWN:
                return event.jbutton.button;
			
			default:
				inputValue = -1;
		}
	}*/
}

